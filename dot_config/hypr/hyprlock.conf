background {
    monitor =
    path = ~/Pictures/wallpapers/wallpaper.jpg
    blur_passes = 2
}

input-field {
    monitor =
    size = 300, 50
    position = 0, -80
    halign = center
    valign = center
    placeholder_text = password
    border_color = rgba(cba6f7ff)
    rounding = 8
}
